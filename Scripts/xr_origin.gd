extends XROrigin3D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	#Input.get_action_strength("")
	pass


#func _on_right_hand_button_pressed(name):
	#print(name)


#func _on_right_hand_input_float_changed(name, value):
	#if(name=="grip" && value>0.9):
		#$RightHand/GPUParticles3D.emitting = true
	#else:
		#$RightHand/GPUParticles3D.emitting = false
	#print(name,value)
